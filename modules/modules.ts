// typescript is modular, we can divide our code up over several modules
// In angular 2 we then use "import {} from " to access the modules
// We export a class, interface, variable,... by adding export keyword

export class ExportedClass {
    // this class is exported
}
