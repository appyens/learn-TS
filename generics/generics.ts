// Generics are types which can hold several types
let numberArray: Array<number>; // this array will only accept numbers

// try to initialize it with strings
// numberArray = ['test'];
numberArray = [1, 2, 3];
